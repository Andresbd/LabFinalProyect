type Tequilera @model {
  id: ID!
  name: String!
  tequilas: [Tequilas] @connection(name: "Destilados")
}
type Tequilas @model {
  id: ID!
  nombre: String!
  anio: String!
  marca: Tequilera @connection(name: "Destilados")
  
}